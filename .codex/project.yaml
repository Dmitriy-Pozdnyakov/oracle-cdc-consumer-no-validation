project_name: oracle-cdc-consumer-no-validation
paths:
  scope_root: .
stack:
  - python
  - docker
  - kafka
runtime:
  start_command: docker compose run --rm <service-name>
policies:
  bad_message_policy_default: strict
documentation:
  function_docstring_language: ru
  readme_update_policy: always_when_behavior_changes
